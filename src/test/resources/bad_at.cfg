# This is test_at.cfg but with classes in default packages and fields names that match primitive values
public-f a finalPrivateField
protected a I

# each of the 3 I's here should be lexed as different token types
public I I(I)V

public I
